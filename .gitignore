# ── Python / 백엔드 ───────────────────────────────────────────
backend/.venv/
__pycache__/
*.pyc
backend/.env

# ── Node / 프론트엔드 ─────────────────────────────────────────
frontend/node_modules/
frontend/.expo/
frontend/dist/
frontend/web-build/
frontend/expo-env.d.ts

# ── 환경 변수 (민감 정보 — 절대 커밋 금지) ───────────────────
frontend/.env*.local

# ── Expo / EAS 네이티브 생성 파일 (prebuild 로 언제든 재생성 가능) ──
frontend/android/
frontend/ios/

# ── 인증서 / 키파일 ──────────────────────────────────────────
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.pem

# ── 기타 ─────────────────────────────────────────────────────
# 초기 파일 압축본 — 저장소에 포함 불필요
files.zip
files/
.DS_Store
*.orig.*
npm-debug.*
yarn-debug.*
yarn-error.*
*.tsbuildinfo
.kotlin/
