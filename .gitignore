# ── Python / 백엔드 ───────────────────────────────────────────
backend/.venv/
__pycache__/
*.pyc
backend/.env

# ── Node / 프론트엔드 ─────────────────────────────────────────
frontend/node_modules/
frontend/.expo/
frontend/dist/
frontend/web-build/
frontend/expo-env.d.ts

# ── 환경 변수 (민감 정보 — 절대 커밋 금지) ───────────────────
frontend/.env*.local

# ── Expo / EAS 네이티브 생성 파일 (prebuild 로 언제든 재생성 가능) ──
frontend/android/
frontend/ios/

# ── 인증서 / 키파일 ──────────────────────────────────────────
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.pem

# ── IDE / 에디터 설정 ─────────────────────────────────────────
# Claude Code 프로젝트 설정 — 개인 IDE 설정이므로 저장소 제외
**/.claude/
# VSCode workspace 설정은 팀 공유 목적이면 커밋 가능, 개인 설정이면 제외
# .vscode/

# ── 기타 ─────────────────────────────────────────────────────
# 초기 파일 압축본 — 저장소에 포함 불필요
files.zip
files/
.DS_Store
*.orig.*
npm-debug.*
yarn-debug.*
yarn-error.*
*.tsbuildinfo
.kotlin/
